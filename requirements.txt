# BERTc 的 Python 依赖。装法:
#     uv pip install -r requirements.txt
#
# 两个 C++ 依赖(PieceTokenizer / Wapic)不在这里 —— 它们要 clone 加编译,
# 由 `make -C prepare deps` 装,见 prepare/install_deps.sh。

# --- 模型与训练 -------------------------------------------------------------
# src/ 只依赖 torch。CRF、优化器、LR 调度、整词掩码、safetensors 读取都是
# 自己实现的,所以这一节就一行。
torch>=2.6

# --- 数据获取与加工(prepare/ 和 data/ 用)---------------------------------
numpy>=1.24              # prepare/pretokenize.py 拼接语料
huggingface_hub>=0.23    # data/download.py 下 HF 数据集
pyarrow>=15              # 读 parquet 语料(finewiki / FineWeb-Edu)
pandas>=2.0              # data/process.py 读 cnn_dailymail

# --- 发布(save/ 用)-------------------------------------------------------
# 只有**写** safetensors 需要这个库;发布包里的推理代码用的是自带的
# checkpoint.py(纯 torch),下载模型的人不需要装它。
safetensors>=0.4

# --- 测试用的对照物(可选,但强烈建议装)-------------------------------------
# test_crf.py 和 test_optim.py 拿它们当参照做数值对拍。**不装不会报错,
# 只会打印「未安装,跳过对拍」然后退出码 0** —— 一个跳过的测试不是测试。
pytorch-crf>=0.7             # test_crf.py 的参照
torch-optimi>=0.3.3          # test_optim.py 的参照(v4-Large 就是用它训的)
transformers>=4.40           # test_optim.py 的 LR 调度参照
